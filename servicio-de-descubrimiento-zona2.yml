eureka:
  instance:
    hostname: localhost
    metadataMap:
      zone: zona2
  client:
    serviceUrl:
      defaultZone: http://localhost:3000/eureka/,http://localhost:3002/eureka/
server:  
  port: ${PORT:3001}
