eureka:
  instance:
    metadataMap:
      zone: zona1
  client:
    serviceUrl:
      defaultZone: http://localhost:3001/eureka/
server:
  port: ${PORT:9001}

management:
  endpoints:
    web:
      exposure:
        include: "*"
#
demo:
  nombreArchivo: servicio-cliente-zona1.yml
  propiedadDinamica: 1
app:
  service-name: NOMBRE_SERVICIO_2
