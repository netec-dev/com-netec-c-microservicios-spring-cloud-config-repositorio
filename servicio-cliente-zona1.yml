eureka:
  instance:
    metadataMap:
      zone: zona1
  client:
    serviceUrl:
      defaultZone: http://localhost:3001/eureka/
server:
  port: ${PORT:9001}

#
management:
  security:
    enabled: false
    endpoints:
      web:
        exposure:
          include: "*"
demo:
  nombreArchivo: servicio-cliente-zona1.yml
  propiedadDinamica: Valor dinámico 1
  # Propiedades
  string: Servicio Cliente Con Configuración 1 (MODIFICADO)
  int:
    propiedad: 1

